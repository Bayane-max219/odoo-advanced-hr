<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- ─── TRAINING PLAN ─── -->

    <record id="view_hr_training_plan_form" model="ir.ui.view">
        <field name="name">hr.training.plan.form</field>
        <field name="model">hr.training.plan</field>
        <field name="arch" type="xml">
            <form string="Training Plan">
                <header>
                    <button name="action_start" string="Start Training" type="object"
                            class="btn-primary"
                            invisible="state != 'draft'"/>
                    <button name="action_complete" string="Mark Completed" type="object"
                            class="btn-primary"
                            invisible="state != 'in_progress'"/>
                    <field name="state" widget="statusbar"
                           statusbar_visible="draft,in_progress,completed"/>
                </header>
                <sheet>
                    <group>
                        <group string="Employee &amp; Skill">
                            <field name="employee_id"/>
                            <field name="skill_id"/>
                            <field name="current_level_id"/>
                            <field name="target_level_id"/>
                        </group>
                        <group string="Training Details">
                            <field name="training_type"/>
                            <field name="provider"/>
                            <field name="cost"/>
                            <field name="currency_id"/>
                            <field name="deadline"/>
                            <field name="completion_date"
                                   invisible="state != 'completed'"/>
                        </group>
                    </group>
                    <group string="Notes">
                        <field name="notes" nolabel="1"/>
                    </group>
                </sheet>
                <div class="oe_chatter">
                    <field name="message_follower_ids"/>
                    <field name="message_ids"/>
                </div>
            </form>
        </field>
    </record>

    <record id="view_hr_training_plan_list" model="ir.ui.view">
        <field name="name">hr.training.plan.list</field>
        <field name="model">hr.training.plan</field>
        <field name="arch" type="xml">
            <tree string="Training Plans"
                  decoration-success="state == 'completed'"
                  decoration-warning="state == 'in_progress'">
                <field name="employee_id"/>
                <field name="skill_id"/>
                <field name="current_level_id"/>
                <field name="target_level_id"/>
                <field name="training_type"/>
                <field name="provider"/>
                <field name="cost"/>
                <field name="currency_id" groups="base.group_multi_currency"/>
                <field name="deadline"/>
                <field name="department_id"/>
                <field name="state" widget="badge"
                       decoration-success="state == 'completed'"
                       decoration-warning="state == 'in_progress'"
                       decoration-muted="state == 'cancelled'"/>
            </tree>
        </field>
    </record>

    <record id="view_hr_training_plan_search" model="ir.ui.view">
        <field name="name">hr.training.plan.search</field>
        <field name="model">hr.training.plan</field>
        <field name="arch" type="xml">
            <search string="Training Plans">
                <field name="employee_id"/>
                <field name="skill_id"/>
                <filter string="Planned" name="draft" domain="[('state','=','draft')]"/>
                <filter string="In Progress" name="in_progress" domain="[('state','=','in_progress')]"/>
                <filter string="Completed" name="completed" domain="[('state','=','completed')]"/>
                <separator/>
                <filter string="Overdue" name="overdue"
                        domain="[('state','in',['draft','in_progress']),('deadline','&lt;',context_today().strftime('%Y-%m-%d'))]"/>
                <group expand="0" string="Group By">
                    <filter string="Employee" name="group_employee"
                            context="{'group_by': 'employee_id'}"/>
                    <filter string="Department" name="group_dept"
                            context="{'group_by': 'department_id'}"/>
                    <filter string="Skill" name="group_skill"
                            context="{'group_by': 'skill_id'}"/>
                    <filter string="Status" name="group_state"
                            context="{'group_by': 'state'}"/>
                </group>
            </search>
        </field>
    </record>

    <record id="action_hr_training_plan" model="ir.actions.act_window">
        <field name="name">Training Plans</field>
        <field name="res_model">hr.training.plan</field>
        <field name="view_mode">list,form</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">No training plans yet</p>
            <p>Training plans are created automatically when a skill gap is detected,
               or manually by the HR manager.</p>
        </field>
    </record>

</odoo>
